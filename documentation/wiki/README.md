# Beginner Wiki

This folder contains the separate wiki layer requested for the project.

Run it locally with:

```bash
npm run wiki:serve
```

The wiki uses Gollum so the content stays version-controlled in plain Markdown while still being presented through actual wiki software.
